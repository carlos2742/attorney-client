import { ShowForRoleDirective } from './show-for-role.directive';

describe('ShowForRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowForRoleDirective();
    expect(directive).toBeTruthy();
  });
});
